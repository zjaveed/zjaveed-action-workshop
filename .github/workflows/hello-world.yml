name: Hello World

on: 
  workflow_dispatch:
  
jobs: 
  hello-world:
    name: Hellow World Job
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello World"

      - run: ls -l
      - uses: actions/checkout@v2
      - run: ls -l
      
